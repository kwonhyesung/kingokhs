# FindText Python 포팅 설계 문서

작성일: 2026-08-14 · 작업 PC: 외부(게임 미실행) · 검증 PC: 집 (도사1PC / 격수PC)

## 1. 배경 / 현황 진단

이 저장소에는 FindText 관련 구현이 세 갈래로 흩어져 있었다.

| 파일 | 상태 | 문제 |
|---|---|---|
| `ft.ahk` (4157줄) | 원본, 정답 | AHK v1 전용, 매칭 코어는 x86/x64 MCode(기계어 바이너리)라 그대로 못 가져옴 |
| `find_text.py` | **죽은 코드** | 아무 파일도 import 안 함. 이전에 AI에게 "ft.ahk처럼 만들어줘"라고 시켰던 결과물. 패턴 포맷이 ft.ahk와 다르고(줄바꿈 0/1 텍스트, base64 아님), 매칭은 근사치 비율 비교, O(n⁴) 순수 파이썬 반복이라 사실상 못 씀 |
| `findtext_wrapper.py` | 실사용 중(`svc_monitor.py` 등) | 패턴 포맷은 ft.ahk와 호환(맞게 구현됨)이지만 매칭 함수(`_template_match_native`)가 픽셀별 이중 for문 → 매우 느림. 캡처(화면→패턴 문자열) 기능 자체가 없음 |
| `svc_findtext.py` | 실사용 중(`OCR.py`, 숫자 인식) | cv2.matchTemplate 기반, 빠르지만 근사 유사도 임계값 방식이라 ft.ahk의 정확한 오차 허용(err1/err0) 방식과 다름 |

**결론**: "ft.ahk와 동일한 성능/정확도"를 원한다면 `find_text.py`는 버리고, `findtext_wrapper.py`(포맷은 맞음)의 매칭 코어만 벡터화해서 정확하고 빠르게 고치는 게 최소 변경으로 목표를 달성하는 길.

## 2. 스코프 결정: mode 2 (Gray Threshold)만 포팅

ft.ahk 패턴은 `color` 접두사에 따라 5가지 모드로 갈린다 (`PicInfo()`, ft.ahk:897-898):

| 접두사 | mode | 용도 |
|---|---|---|
| (없음) | 1 | Color 모드 — 픽셀별 RGB 색상 리스트 매칭 |
| `*N` | **2** | **Gray Threshold 모드 — 그레이스케일 임계값 이진화 매칭** |
| `**N` | 3 | Gray Diff 모드 |
| `#N` | 4 | 단일 기준픽셀 상대비교 모드 |
| `##N` | 5 | FindMultiColor / FindShape / FindPic |

저장소의 모든 실제 패턴 데이터를 검사한 결과:

```
temple/patterns.json, spells_config.json → 전부 "*NNN$..." 형식 (mode 2)
```

mode 1/3/4/5는 이 프로젝트에서 **단 한 건도 쓰이지 않음**. 안 쓰는 4개 모드를 포팅하는 건 존재하지도 않는 요구사항에 코드를 쓰는 것(YAGNI) → **mode 2만 정확히, 완전히 포팅**한다. 나중에 다른 모드가 실제로 필요해지면 그때 추가.

## 3. mode 2 알고리즘 정확한 스펙

### 3.1 그레이스케일 변환 (ft.ahk 고정 가중치)

```
gray_full = R*38 + G*75 + B*15      (0 ~ 32640)
gs        = gray_full >> 7          (0 ~ 255, 캡처용 표시값)
```

BGR로 저장된 numpy 배열(`img[:,:,0]=B, [:,:,1]=G, [:,:,2]=R`) 기준:
```python
gray_full = img[:,:,2]*38 + img[:,:,1]*75 + img[:,:,0]*15
```
(`findtext_wrapper.py`에 이미 정확히 구현되어 있었음 — 이 부분은 그대로 재사용)

### 3.2 이진화

```
threshold(thr) 주어지면: c = (thr + 1) << 7
screen_bin = 1 if gray_full < c else 0      # '1' = 글자(전경) 픽셀
```

### 3.3 오차 허용 매칭 (핵심)

패턴 비트맵에서 `len1` = '1' 픽셀 수, `len0` = '0' 픽셀 수.

```
e1_max = (len1 * floor(err1*1024)) >> 10
e0_max = (len0 * floor(err0*1024)) >> 10
if e1_max >= len1: len1 = 0   # 사실상 이 조건 검사 생략
if e0_max >= len0: len0 = 0
```

슬라이딩 윈도우의 각 위치 (x,y)에서:
```
mismatch1(x,y) = 패턴이 '1'인 자리 중 화면이 '0'인 개수   (len1>0일 때만 검사)
mismatch0(x,y) = 패턴이 '0'인 자리 중 화면이 '1'인 개수   (len0>0일 때만 검사)
매치 성립 ⟺ mismatch1 <= e1_max AND mismatch0 <= e0_max
```

### 3.4 벡터화 (성능 고침)

기존 코드는 이 계산을 Python 이중 for문(각 윈도우 위치마다 O(pw·ph))으로 돌려서 640×360 화면 + 20×25 패턴 기준 수억 회 연산이 걸림. `mismatch1`/`mismatch0`는 사실 **상관관계(cross-correlation)** 로 표현 가능:

```
corr1(x,y) = Σ screen_bin ⊙ pattern1_mask   (윈도우 내적)
corr0(x,y) = Σ screen_bin ⊙ pattern0_mask

mismatch1(x,y) = len1 - corr1(x,y)      # pattern=1인데 screen=0인 개수
mismatch0(x,y) = corr0(x,y)             # pattern=0인데 screen=1인 개수 (바로 나옴)
```

`corr1`, `corr0`는 `cv2.matchTemplate(..., cv2.TM_CCORR)` 한 번 호출로 이미지 전체에 대해 **동시에** 계산됨 (OpenCV 내부에서 FFT/직접상관 최적화, C로 실행). 결과는 정확히 정수(픽셀 개수)이므로 float32 정밀도 문제 없음(패턴 크기가 커봐야 수백 픽셀 → 2^24 한계에 한참 못 미침).

→ **알고리즘은 ft.ahk와 100% 동일(픽셀 단위 정확 매칭), 속도만 Python 반복 대신 OpenCV 벡터 연산으로 개선.** 새 의존성 없음(cv2는 이미 사용 중).

## 4. base64 패턴 포맷 (인코딩 왕복)

ft.ahk 패턴 문자열 문법: `|<comment>*threshold$width.base64data`

- 문자셋: `0123456789+/ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz` (63자, 표준 base64와 다른 순서)
- 디코딩: 문자 1개 → 6비트, 이어붙인 뒤 끝의 `10*` 패턴(정지비트) 제거 → 실제 픽셀 비트열
- **인코딩(기존에 없던 것, 이번에 추가)**: 비트열을 6의 배수로 맞추기 위해 `"100000"`에서 필요한 만큼(1~6자) 잘라 뒤에 붙인 뒤 6비트씩 끊어 문자로 변환

이 왕복이 정확해야 GUI에서 캡처한 패턴을 `temple/patterns.json` 등 기존 파일에 그대로 추가해도 ft.ahk/기존 파이썬 디코더 양쪽에서 동일하게 읽힌다.

## 5. 자동 임계값(threshold) 탐지

ft.ahk 캡처 도구(Gui "CaptureS")는 임계값을 안 주면 반복 등분 평균법(isodata와 동일 계열)으로 자동 산출한다 (ft.ahk:1936-1958). 이번 포팅에서 동일 알고리즘을 그대로 이식해서, GUI에서 "자동" 버튼을 누르면 ft.ahk와 같은 임계값이 나오게 한다.

## 5.5 여백 자동 크롭 + 1px 미세조정 (사용자 결정: 새 GUI로 완전 전환 + 즉시 이식)

기존에는 실제 .ahk 환경에서 ft.ahk 캡처 도구를 돌려 여백을 자동으로 잘라내고
(CutUp/CutDown), 1px 단위로 선택 영역을 미세조정해서 패턴을 뽑아왔다. 이번에
AHK 없이도 동일한 결과를 낼 수 있도록 두 기능을 Python GUI에 이식했다.

- **여백 자동 크롭** (`FindText.auto_crop_margins`, ft.ahk `GetTextFromScreen()` 이식,
  ft.ahk:1967-1976): 위/아래에서 한 행 전체가 전부 배경(`0`)이거나 전부 전경(`1`)인
  동안 계속 잘라낸다. ft.ahk 기본값(`cut:=1`)과 동일하게 `capture_pattern(..., cut=True)`가
  기본. 전부 잘려서 빈 문자열이 되는 극단적인 경우(완전 단색 ROI)엔 크롭을 포기하고
  원본을 그대로 사용 — ft.ahk에는 없는 안전장치지만, 빈 패턴을 만드는 것보다 낫다는
  판단으로 추가.
- **1px 미세조정**: GUI에 4변(좌/상/우/하) 각각 `-1`/`+1` 버튼 + 리셋. 최초 드래그
  선택 영역(`_orig_region`)을 기준으로 각 변의 누적 조정량만 추적하므로, 여러 번
  조정해도 원본 대비 정확한 픽셀 단위 상태를 유지하고 리셋으로 완전 복원 가능.

## 5.6 색상 매칭 (패턴+색상 동시 검색, 멀티서치)

사용자 요구: "동일한 패턴의 이미지 + 다른 색상", "동일 패턴 + 다른 색상 멀티서치",
"1패턴 1색상(흑백) + 멀티서치"를 도트게임 캐릭터/아이콘 스프라이트에 쓰고 싶음.
스프라이트는 사진과 달리 색상이 대체로 평평(flat)하므로, 색상 오차는 **채널별
절대차(±N per R/G/B)** 로 계산하기로 함(유클리드 거리보다 단순하고 빠르며, ft.ahk
DRDGDB 관례와도 개념이 가깝다).

ft.ahk mode 1(Color 모드)이 원래 이 기능이지만, 실제 문법(`RRGGBB-DRDGDB` 이중
인코딩)이 손으로 다루기엔 너무 난해해서, 같은 개념을 이 엔진만의 훨씬 읽기 쉬운
문법으로 새로 정의했다 (ft.ahk와는 호환 안 됨 — 애초에 mode1은 이 저장소에서
쓰인 적이 없어서 호환성을 지킬 이유가 없었음, §2 참고).

**새 문법**: `|<comment>@RRGGBB~TOL,RRGGBB2~TOL2$width.base64shape`
- `@` 로 시작하면 색상 모드. `~` 뒤 숫자가 채널별 허용오차.
- `,` 로 색상 여러 개 나열 = OR (그중 하나만 맞아도 매치) → "동일 패턴 + 다른
  색상 이미지" 요구사항이 이 하나의 항목으로 처리됨.
- shape(모양) 비트맵은 흑백 모드와 완전히 동일한 방식(threshold 이진화 +
  base64)으로 인코딩 — "잉크"로 판정된 픽셀들만 색상 검사 대상이 되고, 배경
  픽셀 색은 검사하지 않는다(캐릭터 뒤 타일이 달라도 매치되도록 의도적 설계).

**"동일 패턴 + 다른 색상 멀티서치"(구분 필요)**: 같은 모양을 색상만 바꿔 `comment`를
다르게 지정한 패턴을 여러 개 만들고 `|`로 이어붙이면 됨 — 기존에 이미 있던
다중 패턴(`|`) 인프라를 그대로 재사용, 새 코드 없이 해결됨. 결과의 `id` 필드로
어떤 색상이 매치됐는지 구분.

**엔진 추가** (`findtext_wrapper.py`):
- `sample_ink_color(roi_bgr, threshold=None)` — ROI의 잉크 픽셀 평균 RGB 추출(GUI
  색상 후보 기본값 자동 제안용)
- `capture_pattern_color(roi_bgr, colors, threshold=None, comment="", cut=True)`
- `_parse_color_pattern` / `_template_match_color` — 매칭 알고리즘은 mode2와 같은
  cv2.matchTemplate 상관연산 트릭을 재사용(스크린의 "색상 일치 마스크"를 잉크
  마스크와 상관연산 → 배경 흑백 판정 대신 색상 판정만 바뀐 것)
- `find_text()`가 파싱된 패턴의 `mode`가 `'color'`인지 보고 자동으로 올바른
  매처로 분기

**GUI 추가** (`ft.py`):
- "매칭 모드" 라디오(흑백/색상). 색상 선택 시 색상 후보 목록 UI 표시: HEX 입력 +
  실시간 스와치 미리보기 + 허용오차 + "ROI에서 색 추출"(자동 제안) + 후보 추가/삭제
- **패턴 목록 빌더**: "+ 현재 패턴을 목록에 추가" → 여러 패턴(모양/색상 자유
  조합)을 쌓았다가 "목록 결합 → 패턴창에 적용"으로 `|` 결합 — 위 세 시나리오
  전부 이 메커니즘 하나로 처리(별도 로직 안 만듦, 재사용)
- 화면 오버레이/미리보기에 결과별 `id` 라벨 표시 → 여러 색상/모양이 섞인 결과에서
  어떤 게 뭔지 바로 구분

**검증**: 합성 이미지로 5개 테스트 추가(정확 색상 매치, 오차밖 불일치, 다중색상
OR, 캡처 왕복, `|` 결합 멀티서치 태그 구분) — 전부 통과.

## 6. 변경 파일

1. **`findtext_wrapper.py`** (수정)
   - `_template_match_native`: 이중 for문 → `cv2.matchTemplate` 벡터 연산으로 교체 (알고리즘 동일, 속도만 개선). 디버그 print 잔재 제거.
   - `bit2base64` 추가 (인코더, 기존엔 디코더만 있었음)
   - `auto_threshold` 추가 (ft.ahk의 반복 등분 평균법)
   - `capture_pattern(roi_bgr, threshold=None, comment="")` 추가 — ROI를 즉시 `|<comment>*thr$w.base64` 문자열로 변환

2. **`find_text.py` → `ft.py`로 교체 및 파일명 변경** (아무 곳에서도 import되지 않아 이름 변경에 위험 없음)
   - 기존 내용(별도 부정확 엔진 + CustomTkinter GUI, 아무도 안 씀) 폐기
   - 새 내용: `findtext_wrapper.FindText`를 실제 엔진으로 사용하는 Tkinter 기반 캡처/테스트 GUI
     - 화면 드래그 선택 → 실시간 이진화 미리보기(임계값 슬라이더 + 자동 버튼)
     - 생성된 패턴 문자열 표시 + 클립보드 복사
     - "매칭 테스트": 패턴 문자열로 이미지(재캡처 또는 저장된 스크린샷 파일) 위에서 실제로 찾아서 박스 표시 — **오늘 이 PC에서도(바탕화면 아무 텍스트로) 매칭 로직 자체를 검증 가능**, 집에서는 동일 GUI로 실제 게임 화면(OBS 가상카메라 프레임 캡처 후 저장한 파일, 또는 화면 직접)에 대해 그대로 재사용

3. 새 의존성 없음: tkinter/PIL/mss/cv2/pyperclip 전부 이미 설치돼 있음 확인.

## 7. 건드리지 않는 것 (범위 밖, 이유 명시)

- **`svc_monitor.py`의 `_recognize_with_findtext`(2442줄)**: `_template_match_native` 반환 딕셔너리를 `for x, y, digit in all_matches:` 로 3개 값 언패킹하는데 실제 반환은 5개 키 딕셔너리라 **호출되면 무조건 예외** 발생하는 죽은 코드(grep 결과 정의부 외에 호출부 없음, 즉 아무도 안 부름). 오늘 못 고치는 이유: 게임 미실행 상태라 실제 동작 검증 불가 + 사용자가 이번 작업 범위로 요청한 건 "FindText 엔진 자체"이지 이 특정 호출부가 아님. **참고용으로만 남겨둠. 집에서 이 함수를 실제로 쓸 계획이면 반환 형식(x,y,w,h,id 5개 키)에 맞게 언패킹부터 고쳐야 함.**
- **map_info ROI 정확 감지 (사용자 요청 1번)**: 이번 작업은 그 전 단계(정확한 FindText 엔진 + 패턴 캡처 GUI) 준비. map_info는 이 엔진이 준비된 뒤, GUI로 각 맵 이름을 캡처해서 패턴화 → `find_text()`로 대조하는 방식으로 이어감 (다음 작업).
- mode 1/3/4/5 (FindPic, FindMultiColor, FindShape, 단일픽셀 비교) — §2에서 설명한 대로 이 프로젝트에서 미사용, 미구현.

## 8. 검증 계획

### 지금 이 PC에서 (게임 없이) 확인할 것
1. `ft.py` 실행 → GUI 뜨는지, 바탕화면 임의의 텍스트(예: 메모장 글자)를 드래그 캡처
2. 생성된 패턴 문자열이 `|<>*NNN$W.base64...` 형식인지 육안 확인
3. "매칭 테스트"로 같은 화면 재검색 → 캡처했던 위치에서 1개 매치되는지 (좌표 일치 확인)
4. `temple/patterns.json`에 있는 기존 숫자 패턴 하나를 읽어서, 그 패턴으로 만든 테스트 이미지(비트맵을 직접 그려서 저장) 매칭 시 100% 일치하는지 — 알고리즘 회귀 테스트
5. 캡처 → 저장 → 다시 로드했을 때 동일 패턴 문자열이 나오는지 (인코딩 왕복 확인)

### 집(실제 게임 PC)에서 확인할 것
1. OBS 가상카메라로 들어오는 실제 게임 화면 스틸컷을 캡처해서 `ft.py` GUI에 이미지 파일로 로드 → map_info ROI 부분을 드래그 캡처해서 맵 이름 패턴 생성
2. 생성한 패턴으로 `find_text()` 호출 시 게임 실행 중 매 프레임 정상적으로 현재 맵 이름이 인식되는지
3. 기존 `svc_findtext.py` 기반 숫자 인식(exp, x, y 등)이 이번 변경으로 영향받지 않았는지 회귀 확인 (해당 파일은 이번에 손대지 않았으므로 원칙적으로 무영향이어야 함 — 확인만)
4. 속도 비교: 기존 이중 for문 대비 새 벡터화 매칭의 프레임당 처리 시간
