# 시스템 구성 설명

## 1. MonitorSvc (PatternMatcher OCR)

### 개요
MonitorSvc는 PatternMatcher를 사용하여 게임 화면에서 OCR을 수행하는 서비스입니다.

### 구성 요소
- **PatternMatcher**: 템플릿 매칭 기반 OCR 엔진
- **ReaderThread**: 백그라운드 스레드에서 실시간 캡처 및 OCR 수행

### 캡처 방식
- **OBS 가상 카메라**: cv2.VideoCapture를 사용하여 OBS 가상 카메라에서 캡처
- **인덱스 자동 감지**: 0, 1, 2 순서대로 시도하여 OBS 카메라 찾기
- **해상도 고정**: 1280x720으로 고정하여 연산 부하 감소
- **Retry 루프**: 카메라 연결 끊김 시 자동 재연결 (최대 5회)

### OCR 방식
- **템플릿 매칭**: templates/ 폴더의 이미지와 매칭
- **RGB 그레이스케일 이진화**: HSV Value 대신 RGB 그레이스케일 사용 (색상 정보 유지)
- **NMS 기반 다중 매칭**: 숫자 인식에 NMS 적용
- **매칭 임계치**: 0.68 (0.72에서 낮춤)

### ROI (Region of Interest)
- config.json에서 정의된 영역에서 OCR 수행
- 숫자 필드: hp, mp, exp, money, x, y
- 타겟 정보: target_info (monsters 폴더)
- 유저 정보: user_info (users 폴더)
- 상태 정보: stat_info (status 폴더)
- 아이템 스캔: items_scan (items 폴더)

### 안티치트 우회
- **랜덤화**: 캡처 간격 16~150ms 랜덤화
- **활성 상태 확인**: 게임 창 포커스 확인 후 캡처
- **표준 카메라 입력**: OBS 가상 카메라는 표준 카메라 입력으로 감지 어려움

---

## 2. LogicSvc (FSM 로직)

### 개요
LogicSvc는 유한 상태 머신(FSM)을 사용하여 게임 로직을 제어하는 서비스입니다.

### 상태 (AppStatus)
- SETUP: 초기 설정 상태
- IDLE: 대기 상태
- EMERGENCY_HEAL: 긴급 치유 상태
- MANA_REGEN: 마나 회복 상태
- BUFFING: 버프 상태
- DEBUFFING: 디버프 제거 상태
- COMBAT: 전투 상태
- SCANNING: 스캔 상태
- STUCK_ESCAPE: 막힘 탈출 상태

### 기능
- 스킬 쿨다운 관리
- HP/MP 트리거 감지
- 타겟 타겟팅
- 이동 경로 계산
- 네비게이션 제어

---

## 3. AHK vs PatternMatcher 분석

### AHK FindText
- **캡처 방식**: GDI+ (CPU 기반)
- **이진화**: RGB 그레이스케일
- **템플릿 매칭**: 단순 템플릿 매칭
- **속도**: 16~100ms
- **인식률**: 높음

### PatternMatcher (Python)
- **캡처 방식**: OBS 가상 카메라 (GPU 가속)
- **이진화**: RGB 그레이스케일 (개선됨)
- **템플릿 매칭**: NMS 기반 다중 매칭
- **속도**: 16~150ms (랜덤화)
- **인식률**: 개선됨 (이진화 방식 변경, 임계치 낮춤)

### 인식률 차이 원인
1. **이진화 방식**: AHK는 RGB 그레이스케일, PatternMatcher는 HSV Value (개선됨)
2. **매칭 임계치**: AHK는 낮은 임계치, PatternMatcher는 높은 임계치 (개선됨)
3. **템플릿 매칭 알고리즘**: AHK는 단순 매칭, PatternMatcher는 NMS 기반

---

## 4. 비활성 캡처 특징

### 특징
- 창이 활성 상태가 아니어도 캡처 가능
- 윈도우 API(win32gui)를 사용하여 창 핸들 얻기
- 사용자가 다른 작업을 하면서도 캡처 가능
- 안티치트에 감지될 수 있음 (비정상적인 패턴 감지)

### 비정상적인 패턴 예시
1. **빈번한 API 호출**: win32gui 함수를 아주 짧은 간격(10ms 미만)으로 반복 호출
2. **비활성 캡처 패턴**: 게임 창이 비활성 상태에서도 캡처 시도 반복
3. **고정된 간격 캡처**: 매번 정확히 같은 간격으로 캡처
4. **비정상적인 프로세스 동작**: 게임과 관련없는 프로세스에서 게임 메모리 접근
5. **후킹 패턴**: 게임 프로세스에 코드 주입 시도
6. **비정상적인 키 입력**: 하드웨어 입력 없이 소프트웨어로 키 입력 반복

### 현재 방법
- 활성 상태에서만 캡처 (안티치트 우회)

---

## 5. 안티치트 SOTA 우회방법

### 적용된 방법
1. **캡처 간격 랜덤화**: 16~150ms 범위에서 랜덤 간격으로 캡처
2. **활성 상태 확인**: 게임 창 포커스 확인 후 캡처
3. **하드웨어 입력**: ESP32-S3 하드웨어 입력 유지
4. **표준 카메라 입력**: OBS 가상 카메라는 표준 카메라 입력으로 감지 어려움

### 고려된 방법 (권장하지 않음)
1. **프로세스 위장**: 프로세스 이름/설명 위장
   - 위험성: Windows 보안 정책 위반 가능성, 안티치트 감지 가능
   - 권장 여부: 권장하지 않음 (위험성 높음)

2. **메모리 랜덤화**: 메모리 패턴 랜덤화
   - 위험성: 프로그램 안정성 저하, 디버깅 어려움
   - 권장 여부: 권장하지 않음 (복잡함, 효과 미미)

### 결론
랜덤화, 활성 상태 확인, 하드웨어 입력만으로 충분한 안티치트 우회가 가능합니다.
